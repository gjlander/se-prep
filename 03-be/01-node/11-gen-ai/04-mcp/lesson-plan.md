# MCP

- `.vscode` is the folder `mcp.json` is the file
- absolute path issue with bash - replace `/c/` with `C:`
- Repo needs to be root open directory in VS Code window
- Make sure it is running in `mcp.json` file
- remove `<>` from URL
- need to restart MCP server after building
- always remove `<>` from URL
- query / command not working (you can't copy/paste - have to type it)
