# Deployment

## Create a Resource Group

- Choose `West Europe` instead of `Germany West Central`

## Create Azure SQL Database with Free Serverless Offer

-

## Deploy Using VS Code Azure Tools

- `Resource Providers` is under `Settings`

## Get Your Database Connection String

- `Show Network Settings` is under `Security` -> `Networking`

## Exercise

- Can use same server as tiny-api
- You can add nested properties with a double underscore (i.e `Jwt__Key`)
